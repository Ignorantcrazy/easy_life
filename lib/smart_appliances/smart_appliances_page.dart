import 'package:flutter/material.dart';

class SmartAppliancesPage extends StatelessWidget {
  SmartAppliancesPage({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Text('SmartAppliancesPage');
  }
}
